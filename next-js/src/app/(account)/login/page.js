'use client'//directive
export default function LoginPage() {
    return (
        <>
            <h1>Login Page</h1>
            <button onClick={() => alert('Login')}>Login</button>
        </>
    )
}